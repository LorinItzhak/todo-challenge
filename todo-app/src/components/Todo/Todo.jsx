import React, {useRef} from 'react';
import { v4 as uuidv4 } from 'uuid';
import { useDrag, useDrop } from "react-dnd";


export const Todo = ({todo, checkToggle, onDelete, index, moveTodo}) => {

    const red = useRef(null);


    
};